
   <% @artists.each do |artist| %>
   <br/>
   <%= link_to artist.name, artist_path(artist) %><br/>
   <%= pluralize(artist.song_count, 'song') %><br/>

<% artist.songs.each do |song| %>
   <%= link_to song.title, song_path(song) %><ul><br/>
   <% end %>
   <% end %>
</ul>
